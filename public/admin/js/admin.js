

// $('.nav li').on('click', 'a', function (e) {
//     e.preventDefault();
//     e.stopPropagation();
//     // only do the following if the clicked link isn't already active
//     if(!$(this).closest('li').hasClass('active-menu')) {
//         $(this).closest('ul').find('.active-menu').removeClass('active-menu');
//         $(this).closest('li').addClass('active-menu');
//
//         // load in your content via ajax, etc.
//     }
// });
